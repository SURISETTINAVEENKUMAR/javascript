protect_email = function (user@email) {
    var  splitmail, part1, part2, part3;
    splitmail = user_email.split("@");
    part1 = splitmail[0];
    part2 = part1.substring(0, 3);
    part3 = splitmail[1];
    return part2 + "...@" + part3;
}
